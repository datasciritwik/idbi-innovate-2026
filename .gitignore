.venv/
venv/
__pycache__/
*.pyc
node_modules/
.env
.DS_Store

# Generated synthetic data — baked into backend at deploy time, not versioned
data/output/

# Internal planning doc — not for the public repo
idbi-innovate-2026-submission-plan.md

# Project docs folder
/docs/

# Runtime SQLite databases (per-IP quota ledger, session state)
backend/data/*.db

# Self-hosted model deployment code — deployed straight to Modal from here,
# never pushed to the public repo
modal_deploy/
